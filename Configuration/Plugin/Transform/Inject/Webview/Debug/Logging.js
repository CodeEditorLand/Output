const E="/* __LAND_WEBVIEW_DEBUG_INJECT__ */",f="		const isSafari = (",D=`/* __LAND_WEBVIEW_DEBUG_INJECT__ */
const WV_ID = (() => {
try {
return new URL(location.toString()).searchParams.get('id') || '';
} catch (_) { return ''; }
})();
const WV_T = () => \`[\${WV_ID}][\${(Date.now() - performance.timeOrigin).toFixed(0)}ms]\`;
const DEBUG_WV = (...A) => console.log('[WebviewDebug]', WV_T(), ...A.map(a => typeof a === 'object' ? JSON.stringify(a) : String(a)));
DEBUG_WV('INDEX_BOOT', { ua: navigator.userAgent.slice(0, 120), origin: location.origin, hostname: location.hostname, protocol: location.protocol, cryptoSubtle: !!globalThis.crypto?.subtle, crossOriginIsolated: globalThis.crossOriginIsolated, referrer: document.referrer });

`,S=/\/vs\/workbench\/contrib\/webview\/browser\/pre\/index\.html$/,L={Kind:"Transform",Name:"InjectWebviewDebugLogging",Match:({Path:n})=>S.test(n),Transform({Source:n}){if(n.includes(E))return{Kind:"Unchanged"};const o=n.indexOf(f);if(o<0)return{Kind:"Unchanged"};let e=n.slice(0,o)+D+n.slice(o);const s="navigator.serviceWorker.register(swPath";e.includes(s)&&(e=e.replace(s,"DEBUG_WV('SW_REGISTER_ATTEMPT', { path: swPath }); navigator.serviceWorker.register(swPath"));const r=".then(async registration => {";e.includes(r)&&(e=e.replace(r,".then(async registration => { DEBUG_WV('SW_REGISTER_OK', { controller: !!navigator.serviceWorker.controller });"));const c=".catch(error => {";e.includes(c)&&(e=e.replace(c,".catch(error => { DEBUG_WV('SW_REGISTER_FAIL', error.message || String(error));"));const a="const start = (/** @type {string} */ parentOrigin) => {";e.includes(a)&&(e=e.replace(a,"const start = (/** @type {string} */ parentOrigin) => { DEBUG_WV('SIGNAL_START', { parentOrigin, hostname: location.hostname, origin: location.origin });"));const i="window.parent.postMessage({ target: ID, channel: 'webview-ready'";e.includes(i)&&(e=e.replace(i,"DEBUG_WV('SIGNAL_OK', 'posting webview-ready'); window.parent.postMessage({ target: ID, channel: 'webview-ready'"));const l="throw new Error(`Expected '${parentOriginHash}' as hostname or subdomain!`);";e.includes(l)&&(e=e.replace(l,"DEBUG_WV('SIGNAL_FAIL', { hashMismatch: true, hostname, parentOriginHash }); throw new Error(`Expected '${parentOriginHash}' as hostname or subdomain!`);"));const g="hostMessaging.onMessage('content', async";e.includes(g)&&(e=e.replace(g,"DEBUG_WV('REAL_CONTENT', 'content handler fired'); hostMessaging.onMessage('content', async"));const d="const newDocument = toContentHtml(data);";e.includes(d)&&(e=e.replace(d,`const newDocument = toContentHtml(data); DEBUG_WV('CONTENT_PROCESSED', { docLen: newDocument.length, hasRoot: newDocument.includes('id="root"'), hasModuleScript: newDocument.includes('type="module"'), snippet: newDocument.slice(0, 400) });`));const p="hostMessaging.onMessage('content'";e.includes(p)&&(e=e.replace(p,"DEBUG_WV('CONTENT_EVENT', 'content handler invoked (unloadMonitor)'); hostMessaging.onMessage('content'"));const h="newFrame.src = `./fake.html";e.includes(h)&&(e=e.replace(h,"DEBUG_WV('INNER_FRAME_FAKE', 'setting src to fake.html'); newFrame.src = `./fake.html"));const u="function onFrameLoaded(contentDocument) {";e.includes(u)&&(e=e.replace(u,"function onFrameLoaded(contentDocument) { DEBUG_WV('INNER_FRAME_LOADED', { readyState: contentDocument.readyState });"));const w="contentDocument.write(newDocument);";e.includes(w)&&(e=e.replace(w,"DEBUG_WV('INNER_WRITE', { htmlLen: newDocument.length }); contentDocument.write(newDocument);"));const m="csp.setAttribute('content', newCsp);";e.includes(m)&&(e=e.replace(m,"csp.setAttribute('content', newCsp); /* Land font-src wildcard fallback for WKWebView */ if (!/font-src\\s+\\*/.test(newCsp)) { try { csp.setAttribute('content', newCsp.replace(/(font-srcs+)([^;]+)/, '$1$2 *')); } catch(_) {} }"));const _="if (options.allowScripts) {";return e.includes(_)&&(e=e.replace(_,"/* Land forced API injection */ DEBUG_WV('ALLOW_SCRIPTS', { allowScripts: options.allowScripts, hasState: !!data.state }); const defaultScript = newDocument.createElement('script'); defaultScript.id = '_vscodeApiScript'; defaultScript.textContent = getVsCodeApiScript(options.allowMultipleAPIAcquire, data.state); try { newDocument.head.prepend(defaultScript); } catch(_e) {} if (options.allowScripts) {")),e===n?{Kind:"Unchanged"}:{Kind:"Rewrite",Source:e}}};var W=L;export{W as default};
