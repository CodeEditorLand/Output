export default (Prefix: string) => [
	// --- 9. Other Potential Excludes (Lower Confidence - Test!) ---
	// `${Prefix}/platform/profiling`,
	// `${Prefix}/platform/cssDev`,
	// `${Prefix}/base/common/marked`, // If no core markdown rendering needed
	// `${Prefix}/base/browser/dompurify`, // If no HTML sanitization needed by core
];
